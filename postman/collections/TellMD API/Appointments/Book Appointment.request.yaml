$kind: http-request
name: Book Appointment
method: POST
url: "{{baseUrl}}/api/v1/appointments"
auth:
  type: bearer
  credentials:
    token: "{{accessToken}}"
body:
  type: json
  content: |-
    {
      "doctorId": "cmpny9tz900033kglbrnl6ssl",
      "scheduledAt": "2026-06-10T10:00:00.000Z",
      "notes": "Optional note for the doctor about symptoms or concerns."
    }
order: 1000
