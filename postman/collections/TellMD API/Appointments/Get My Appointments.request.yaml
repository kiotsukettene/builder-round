$kind: http-request
name: Get My Appointments
method: GET
url: "{{baseUrl}}/api/v1/appointments?page=1&limit=10"
queryParams:
  page: "1"
  limit: "10"
auth:
  type: bearer
  credentials:
    token: "{{accessToken}}"
order: 2000
