$kind: http-request
name: Get Medical Record Detail
method: GET
url: "{{baseUrl}}/api/v1/medical-records/APPOINTMENT_ID_HERE"
auth:
  type: bearer
  credentials:
    token: "{{accessToken}}"
order: 2000
