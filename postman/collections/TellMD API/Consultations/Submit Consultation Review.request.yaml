$kind: http-request
name: Submit Consultation Review
method: POST
url: "{{baseUrl}}/api/v1/consultations/cmpo76h010009dkglo0g8mw7d/review"
body:
  type: json
  content: |-
    {
      "rating": 5,
      "comment": "Very helpful and professional consultation."
    }
auth:
  type: bearer
  credentials:
    token: "{{accessToken}}"
order: 2500
