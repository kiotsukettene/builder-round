$kind: http-request
name: Get Notifications
method: GET
url: "{{baseUrl}}/api/v1/notifications?page=1&limit=20"
queryParams:
  page: "1"
  limit: "20"
auth:
  type: bearer
  credentials:
    token: "{{accessToken}}"
order: 1000
