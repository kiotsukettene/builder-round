$kind: http-request
name: Block Date
method: POST
url: "{{baseUrl}}/api/v1/schedules/blocked-dates"
order: 4000
auth:
  type: bearer
  credentials:
    token: "{{accessToken}}"
body:
  type: json
  content: |-
    {
      "date": "2026-07-04",
      "reason": "Public holiday"
    }
